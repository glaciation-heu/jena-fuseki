apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jena-fuseki.fullname" . }}-test-connection"
  labels:
    {{- include "jena-fuseki.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jena-fuseki.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
